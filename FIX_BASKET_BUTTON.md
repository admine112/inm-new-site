# Инструкция: Как исправить кнопку корзины в header

**Проблема:** Кнопка корзины в header не кликабельна, хотя страница `/basket` работает

**Решение:** CSS или JavaScript блокирует клик. Ниже пошаговая инструкция.

---

## 🔧 Решение 1: Проверка и исправление CSS (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Откройте сайт в браузере
Откройте `https://inmunoflam.com.ua/`

### Шаг 2: Откройте DevTools
Нажмите **F12** или **Ctrl+Shift+I**

### Шаг 3: Найдите элемент корзины
1. Нажмите на иконку "Выбрать элемент" (стрелка в углу DevTools)
2. Наведите на кнопку корзины в header
3. Кликните на неё

### Шаг 4: Проверьте CSS свойства
В правой панели DevTools найдите вкладку **"Styles"** или **"Computed"**

Ищите следующие свойства:

```css
pointer-events: none;    /* ← ПРОБЛЕМА! Блокирует клики */
cursor: default;         /* ← Может быть проблемой */
z-index: -1;            /* ← Элемент под другими */
opacity: 0;             /* ← Невидимый */
```

### Шаг 5: Временное исправление (для теста)
В DevTools → Styles → Найдите проблемное свойство и:

1. Снимите галочку напротив `pointer-events: none`
2. Или добавьте новое правило:
   ```css
   .basket a {
       pointer-events: auto !important;
       cursor: pointer !important;
   }
   ```

### Шаг 6: Проверьте
Попробуйте кликнуть на корзину. Если работает - проблема в CSS!

---

## 🔧 Решение 2: Постоянное исправление в файле CSS

### Найдите файл style.css

**Путь на сервере:** `/templates/shop1/css/style.css`

### Найдите правило для корзины

Ищите в файле:
```css
.basket a {
    /* ... */
}

/* или */

#basket a {
    /* ... */
}
```

### Добавьте или исправьте:

```css
.basket a {
    pointer-events: auto !important;
    cursor: pointer !important;
    z-index: 10;
}
```

### Сохраните и проверьте

Обновите страницу (Ctrl+F5) и проверьте кнопку корзины.

---

## 🔧 Решение 3: Исправление через JavaScript (если CSS не помог)

### Вариант A: Добавить onclick в шаблон

**Файл:** `templates/shop1/main.tpl`  
**Строка:** 345

**Было:**
```html
<a href="/basket/">
```

**Стало:**
```html
<a href="/basket/" onclick="window.location.href='/basket'; return true;">
```

---

### Вариант B: Добавить JavaScript в конец страницы

**Файл:** `templates/shop1/main.tpl`  
**Место:** Перед закрывающим `</body>` (примерно строка 478)

**Добавить:**
```html
<script>
document.addEventListener('DOMContentLoaded', function() {
    var basketLink = document.querySelector('.basket a, #basket a');
    if (basketLink) {
        basketLink.style.pointerEvents = 'auto';
        basketLink.style.cursor = 'pointer';
        basketLink.addEventListener('click', function(e) {
            e.stopPropagation();
            window.location.href = '/basket';
        });
    }
});
</script>
```

---

## 🔧 Решение 4: Проверка JavaScript конфликтов

### Шаг 1: Откройте Console в DevTools
F12 → вкладка **Console**

### Шаг 2: Кликните на корзину
Смотрите, появляются ли ошибки красным цветом

### Шаг 3: Если есть ошибки
Скопируйте текст ошибки и найдите проблемный файл JavaScript

**Частые проблемы:**
- `Uncaught TypeError` - ошибка в коде
- `$ is not defined` - jQuery не загружен
- `preventDefault` - событие блокируется

---

## ✅ Быстрое решение (если ничего не помогает)

### Измените HTML напрямую

**Файл:** `templates/shop1/main.tpl`  
**Строки:** 342-352

**Замените весь блок на:**

```html
<div class="basket" id="basket">
  <a href="/basket" style="pointer-events: auto !important; cursor: pointer !important; display: block;">
    <div class="bs_holder cl_basket_text">
      В вашей корзине <strong>0 товар(ов)</strong><br>
      на сумму <strong>0  грн..</strong>
    </div>
  </a>
</div>
```

**Изменения:**
1. Добавлен `style="pointer-events: auto !important; cursor: pointer !important; display: block;"`
2. Убран слэш в конце `/basket/` → `/basket`

---

## 📋 Чеклист исправления

- [ ] Открыл DevTools (F12)
- [ ] Нашёл элемент `.basket a` через инспектор
- [ ] Проверил CSS свойство `pointer-events`
- [ ] Проверил CSS свойство `cursor`
- [ ] Проверил Console на ошибки JavaScript
- [ ] Попробовал временное исправление в DevTools
- [ ] Если сработало - применил постоянное исправление в `style.css` или `main.tpl`
- [ ] Загрузил исправленный файл на сервер
- [ ] Очистил кеш браузера (Ctrl+F5)
- [ ] Проверил - кнопка корзины кликабельна ✅

---

## 📝 Рекомендация

**Лучший вариант:** Решение 2 (исправление CSS в файле `style.css`)

**Самый быстрый:** Решение 4 (добавить `style="..."` прямо в HTML)

**Если не работает:** Решение 3B (добавить JavaScript)

---

**Дата создания:** 2025-11-22 20:30
