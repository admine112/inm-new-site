# КРИТИЧЕСКИЕ ПРОБЛЕМЫ НА СЕРВЕРЕ - АНАЛИЗ HTML

**Дата:** 2025-11-22 20:55  
**Источник:** HTML код с production-сервера

---

## 🔴 НАЙДЕННЫЕ ПРОБЛЕМЫ

### 1. ПРОБЕЛЫ В HEADER-ССЫЛКАХ (КРИТИЧНО!)

**Расположение:** Header меню (строки 365-375 HTML)

**Проблема:**
```html
<a href="/  состав-инмунофлама">    <!-- 2 пробела перед URL! -->
<a href="/  видеопередачи">          <!-- 2 пробела перед URL! -->
<a href="/o  тзывы">                 <!-- пробел ВНУТРИ слова! -->
```

**Почему не работает:**
- URL `/  состав-инмунофлама` содержит пробелы → браузер кодирует как `/%20%20состав-инмунофлама`
- Такой URL не существует в базе данных
- Результат: 404 ошибка

**Решение:**
Исправить в `templates/shop1/main.tpl` (строки 360-374):
```html
<!-- ПРАВИЛЬНО -->
<a href="/sostav-inmunoflama" title="ЧТО СОБОЙ ПРЕДСТАВЛЯЕТ ИНМУНОФЛАМ">
<a href="/videoperedachi" title="Видеопередачи">
<a href="/otzivi" title="Отзывы">
```

---

### 2. ССЫЛКА "КОНСУЛЬТАЦИИ" В FOOTER (НЕ УДАЛЕНА!)

**Расположение:** Footer меню (строка 545 HTML)

**Проблема:**
```html
<a href="/konsultacii">Консультации</a>  <!-- Страница не существует! -->
```

**Решение:**
Удалить эту ссылку из `templates/shop1/main.tpl` (строка 468)

---

### 3. КОРЗИНА НЕ КЛИКАЕТСЯ

**Расположение:** Header, блок корзины (строка 342 HTML)

**Текущий код:**
```html
<div class="basket" id="basket" onclick="window.location.href='/basket';" style="cursor: pointer;">
```

✅ **Код ПРАВИЛЬНЫЙ!** Onclick присутствует.

**Вероятная причина:**
Поверх корзины наложен другой элемент (`.phone`, `.tmholder` или `.topmenu`), который перехватывает клики из-за большего `z-index`.

**Решение:**
Добавить `z-index` к корзине:
```html
<div class="basket" id="basket" onclick="window.location.href='/basket';" 
     style="cursor: pointer; position: relative; z-index: 100;">
```

**Альтернатива (если не поможет):**
Проверить CSS файл `/templates/shop1/css/style.css` на наличие:
```css
.basket {
    pointer-events: none;  /* ← УДАЛИТЬ если есть */
}
```

---

### 4. ДУБЛИРОВАНИЕ КОНТЕНТА

**Расположение:** Сертификаты и Facebook виджет повторяются дважды

**Проблема:**
Блоки `.cert` и `.comments_box` выводятся 2 раза (строки 400-450 и 500-550)

**Причина:**
Вероятно, в шаблоне есть дублирование кода или переменная `{$center}` содержит повторяющийся контент.

**Решение:**
Проверить `templates/shop1/main.tpl` и удалить дублирующиеся блоки.

---

## ✅ ЧТО РАБОТАЕТ ПРАВИЛЬНО

1. ✅ Товары отображаются корректно (5 товаров на главной)
2. ✅ Кнопки "Купить" имеют правильные обработчики `javascript:buy(1)` и т.д.
3. ✅ CSS подключен корректно
4. ✅ JavaScript файлы загружаются
5. ✅ Страница `/basket` существует и работает

---

## 📋 ЧЕКЛИСТ ИСПРАВЛЕНИЙ ДЛЯ СЕРВЕРА

### Файл: `templates/shop1/main.tpl`

- [ ] **Строки 360-374:** Удалить пробелы из header-ссылок
  ```html
  <a href="/sostav-inmunoflama">
  <a href="/videoperedachi">
  <a href="/otzivi">
  ```

- [ ] **Строка 468:** Удалить ссылку "Консультации" из footer
  ```html
  <!-- УДАЛИТЬ -->
  <a href="/konsultacii">Консультации</a>
  ```

- [ ] **Строка 342:** Добавить z-index к корзине
  ```html
  <div class="basket" id="basket" onclick="window.location.href='/basket';" 
       style="cursor: pointer; position: relative; z-index: 100;">
  ```

- [ ] **Опционально:** Удалить дублирующиеся блоки сертификатов (если есть)

---

## 🔧 ИНСТРУКЦИЯ ПО ПРИМЕНЕНИЮ

### Вариант 1: Исправить локально и загрузить

1. Открыть `www/www/templates/shop1/main.tpl`
2. Найти строки 360-374 (header меню)
3. Заменить:
   - `/  состав-инмунофлама` → `/sostav-inmunoflama`
   - `/  видеопередачи` → `/videoperedachi`
   - `/o  тзывы` → `/otzivi`
4. Найти строку 468 (footer меню)
5. Удалить `<a href="/konsultacii">Консультации</a>`
6. Найти строку 342 (корзина)
7. Добавить `position: relative; z-index: 100;` в style
8. Загрузить файл на сервер

### Вариант 2: Исправить напрямую на сервере

1. Подключиться к серверу через FTP/SFTP
2. Скачать `/public_html/templates/shop1/main.tpl`
3. Открыть в текстовом редакторе
4. Применить исправления (см. выше)
5. Загрузить обратно на сервер
6. Очистить кеш браузера (Ctrl+F5)

---

## 🎯 ПРИОРИТЕТ ИСПРАВЛЕНИЙ

**ВЫСОКИЙ (исправить немедленно):**
1. ✅ Пробелы в header-ссылках - **ВСЕ ССЫЛКИ НЕ РАБОТАЮТ!**
2. ✅ Удалить "Консультации" из footer - **404 ошибка**

**СРЕДНИЙ (исправить при возможности):**
3. ⚠️ Корзина z-index - **кнопка не кликается**

**НИЗКИЙ (не критично):**
4. ℹ️ Дублирование контента - **не влияет на функциональность**

---

**Дата создания:** 2025-11-22 20:56
