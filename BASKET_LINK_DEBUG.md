# Проблема: Ссылка на корзину в header не активна

**Дата:** 2025-11-22 19:13  
**Статус:** Исследование

---

## 🔍 Описание проблемы

На production-сервере ссылка на корзину в header не работает (не кликабельна).

**Расположение:** Header сайта, правый верхний угол  
**HTML-код:** `templates/shop1/main.tpl`, строки 342-352

```html
<div class="basket" id="basket">
  <a href="/basket/">
    <div class="bs_holder cl_basket_text">
      В вашей корзине <strong>0 товар(ов)</strong><br>
      на сумму <strong>0  грн..</strong>
    </div>
  </a>
</div>
```

---

## ✅ Проверка локально

**Тест 1:** Проверка URL `/basket/`
```
HTTP 301 → редирект на /basket
HTTP 200 → страница загружается
```

**Тест 2:** Сравнение с оригиналом из `www.zip`
```
✅ HTML идентичен оригиналу
✅ Ссылка <a href="/basket/"> присутствует
```

**Вывод:** HTML-код корректен, проблема НЕ в шаблоне

---

## 🐛 Возможные причины на production

### 1. CSS блокирует клики
**Проверка:** Откройте DevTools → Elements, найдите элемент `.basket a`

Возможные проблемы:
```css
pointer-events: none;  /* Блокирует клики */
z-index: -1;           /* Элемент под другими */
opacity: 0;            /* Невидимый */
```

**Решение:** Проверьте файл `/templates/shop1/css/style.css`

---

### 2. JavaScript перехватывает клик
**Проверка:** Откройте DevTools → Console, кликните на корзину

Возможные проблемы:
- `event.preventDefault()` в обработчике клика
- Ошибка JavaScript блокирует навигацию

**Файлы для проверки:**
- `/includes/js/cms.js`
- `/templates/shop1/main.tpl` (inline JavaScript)

---

### 3. Страница /basket не существует на сервере
**Проверка:** Откройте напрямую `https://inmunoflam.com.ua/basket`

Если 404:
- Проверьте файл `includes/select_page.php` - должна быть обработка URL `/basket`
- Проверьте `.htaccess` - возможно блокирует доступ

---

## 🔧 Решение

### Шаг 1: Проверьте страницу корзины
Откройте в браузере: `https://inmunoflam.com.ua/basket`

**Если работает:** Проблема в CSS/JavaScript  
**Если 404:** Проблема в роутинге

---

### Шаг 2: Проверьте CSS (если страница работает)

Откройте DevTools → Elements → Найдите:
```html
<a href="/basket/">
```

Проверьте вкладку "Computed" → ищите:
- `pointer-events: none` → удалить
- `z-index: -1` → изменить на `z-index: 1`

---

### Шаг 3: Проверьте JavaScript

Откройте DevTools → Console → Кликните на корзину

**Если ошибка:** Исправьте JavaScript  
**Если нет ошибки:** Проблема в CSS

---

### Шаг 4: Временное решение

Если ничего не помогает, измените ссылку на прямую:

```html
<!-- Было -->
<a href="/basket/">

<!-- Стало -->
<a href="/basket" onclick="window.location='/basket'; return false;">
```

---

## 📋 Чеклист диагностики

- [ ] Открыл `https://inmunoflam.com.ua/basket` напрямую
- [ ] Проверил DevTools → Console на ошибки
- [ ] Проверил DevTools → Elements → CSS свойства ссылки
- [ ] Проверил файл `style.css` на `pointer-events`
- [ ] Проверил `cms.js` на обработчики клика
- [ ] Попробовал добавить `onclick` в ссылку

---

## 📝 Примечания

- Локально ссылка работает корректно
- HTML идентичен оригиналу из архива
- Проблема специфична для production-сервера
- Скорее всего CSS или JavaScript блокирует клик

---

**Последнее обновление:** 2025-11-22 19:15
