# 📚 Документація сайту inmunoflam.com.ua

**Дата створення:** 23 листопада 2025  
**Версія:** 1.0  
**Статус:** Робоча версія

---

## 🖥️ Серверна конфігурація

### Хостинг
- **Хост:** is501201.ftp.tools
- **SSH/FTP:** is501201@is501201.ftp.tools
- **Шлях до сайту:** `/home/is501201/inmunoflam.com.ua/www/`

### PHP версія
- **Сервер:** PHP 5.6.40
- **Важливо:** Код має бути сумісний з PHP 5.6!
  - ❌ НЕ використовувати короткі масиви `[]`
  - ✅ Використовувати `array()`
  - ❌ НЕ використовувати PHP 7+ функції

### База даних
- **Хост:** is501201.mysql.ukraine.com.ua
- **База:** is501201_inm
- **Користувач:** is501201_inm
- **Кодування:** UTF-8

---

## 📁 Структура файлів

```
www/
├── index.php                 # Головний файл
├── .htaccess                # Правила маршрутизації
├── robots.txt               # SEO налаштування
│
├── settings/                # Налаштування
│   ├── conf.php            # Конфігурація БД
│   ├── connect.php         # Підключення до БД
│   ├── main.php            # Головні налаштування
│   ├── functions.php       # Допоміжні функції
│   └── generate_page.php   # Генерація сторінок
│
├── includes/               # Основні модулі
│   └── select_page.php    # Маршрутизація сторінок
│
├── modules/                # Функціональні модулі
│   ├── content/           # Модуль контенту
│   │   ├── main.php       # Реєстрація URL
│   │   └── site/
│   │       └── functions.php  # Функції контенту
│   └── ...
│
└── templates/              # Шаблони
    └── shop1/
        ├── main.tpl       # Головний шаблон
        ├── main.center.tpl # Центральна частина
        └── css/
            ├── style.css  # Основні стилі
            └── mobile.css # Мобільні стилі
```

---

## 🔄 Маршрутизація

### Формати URL

1. **За alias** (рекомендовано):
   - `/videoperedachi` → Сторінка "Видеопередачи"
   - `/otzivi` → Сторінка "Отзывы"
   - `/kontakti` → Сторінка "Контакты"

2. **За ID** (запасний варіант):
   - `/p15` → Сторінка з ID 15
   - `/p25` → Сторінка з ID 25

### Як працює маршрутизація

1. `.htaccess` перенаправляє всі запити на `index.php`
2. `index.php` підключає `select_page.php`
3. `select_page.php` викликає відповідну функцію з `modules/content/main.php`
4. Функція `func_url_search_content()` шукає сторінку в БД за alias
5. Викликається `show_article_full()` для відображення контенту

---

## 🗄️ База даних

### Основні таблиці

#### `content_articles`
Зберігає всі статті та сторінки:
- `articleID` - унікальний ID
- `name` - назва сторінки
- `alias` - URL-адреса (латиниця)
- `h1` - заголовок H1
- `title` - meta title
- `description` - meta description
- `keywords` - meta keywords
- `text` - текст сторінки
- `visible` - видимість (1=видима)

#### `domens_description`
Налаштування домену:
- SEO мета-теги для головної
- Тема оформлення
- Email

---

## 🔧 Налаштування

### Конфігурація БД (`settings/conf.php`)
```php
$glb["db_host"]="is501201.mysql.ukraine.com.ua";
$glb["db_basename"]="is501201_inm";
$glb["db_user"]="is501201_inm";
$glb["db_password"]="(!keSB72a5";
```

### .htaccess правила
```apache
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]
```

---

## 💾 Бекапи

### Розташування бекапів
- **На сервері:** `/home/is501201/inmunoflam.com.ua/backup_working_*.tar.gz`
- **Локально:** `работающая_версия_backup/`

### Останній робочий бекап
- **Файл:** `backup_working_20251123_190052.tar.gz`
- **Розмір:** 24 MB
- **Дата:** 23 листопада 2025, 19:00

### Як створити бекап
```bash
cd /home/is501201/inmunoflam.com.ua
tar -czf backup_working_$(date +%Y%m%d_%H%M%S).tar.gz www/
```

### Як відновити з бекапу
```bash
cd /home/is501201/inmunoflam.com.ua
rm -rf www/
tar -xzf backup_working_YYYYMMDD_HHMMSS.tar.gz
```

---

## 🐛 Вирішення проблем

### Parse error: unexpected '['
**Причина:** Використання PHP 7.4 синтаксису на PHP 5.6 сервері  
**Рішення:** Замінити `[]` на `array()`

### Порожній H1
**Причина:** Поле `h1` порожнє в БД  
**Рішення:** Запустити скрипт `fix_db_h1.php`

### Сторінки не відкриваються
**Перевірити:**
1. Чи є alias в БД?
2. Чи `visible=1`?
3. Чи правильний `.htaccess`?

### Посилання ведуть на /p{ID}
**Причина:** Жорстко прописані посилання в шаблоні  
**Рішення:** Замінити в `main.tpl`:
```
/p3 → /videoperedachi
/p25 → /otzivi
/p17 → /kontakti
```

---

## 📝 Додавання нової сторінки

1. **В адмін-панелі:**
   - Додати статтю
   - Заповнити `name`, `alias`, `h1`, `title`, `description`
   - Встановити `visible=1`

2. **Важливо:**
   - `alias` має бути латиницею
   - `h1` не залишати порожнім
   - Перевірити URL: `https://inmunoflam.com.ua/{alias}`

---

## 🔐 Доступи

### FTP/SSH
- Хост: is501201.ftp.tools
- Логін: is501201
- Пароль: [зберігається окремо]

### База даних
- Хост: is501201.mysql.ukraine.com.ua
- База: is501201_inm
- Логін: is501201_inm
- Пароль: [зберігається окремо]

---

## 📞 Підтримка

При виникненні проблем:
1. Перевірити логи помилок
2. Створити бекап перед змінами
3. Тестувати локально (Docker)
4. Звернутися до документації

---

**Версія документації:** 1.0  
**Останнє оновлення:** 23.11.2025
