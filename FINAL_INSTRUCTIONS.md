# 🚀 ФИНАЛЬНАЯ ИНСТРУКЦИЯ ПО ВОССТАНОВЛЕНИЮ

**ВАЖНО:** Выполняйте шаги строго по порядку! Если пропустить шаг 4 (SQL), ссылки работать НЕ БУДУТ.

---

## 1. ФАЙЛЫ ДЛЯ ЗАГРУЗКИ (FTP)

Загрузите эти файлы на сервер, заменяя существующие:

1. **Шаблон (исправлены ссылки, корзина, дубли):**
   - Файл: `main_FIXED.tpl`
   - Куда: `/www/www/templates/shop1/`
   - **Переименовать на сервере в:** `main.tpl`

2. **Настройки базы данных (для сервера):**
   - Файл: `settings/conf_server.php`
   - Куда: `/www/www/settings/`
   - **Переименовать на сервере в:** `conf.php`

3. **Подключение к базе (для PHP 5.x):**
   - Файл: `settings/connect_server.php`
   - Куда: `/www/www/settings/`
   - **Переименовать на сервере в:** `connect.php`

---

## 2. БАЗА ДАННЫХ (ОБЯЗАТЕЛЬНО!)

**Почему ссылки не работают?**
Потому что в файле мы поменяли ссылки на правильные (латиница), а в базе данных они всё ещё старые (кириллица).

**Как исправить:**
1. Зайдите в phpMyAdmin на хостинге.
2. Выберите базу данных `is501201_inm`.
3. Нажмите вкладку **SQL**.
4. **ШАГ 1:** Скопируйте содержимое файла `fix_all_pages.sql` и выполните его.
5. **ШАГ 2:** Скопируйте содержимое файла `insert_missing_pages.sql` и выполните его.
6. **ШАГ 3:** Скопируйте содержимое файла `update_contacts_new.sql` и выполните его (обновит текст контактов на новый).

**Что сделают скрипты:**
- `fix_all_pages.sql`: Исправит ссылки.
- `insert_missing_pages.sql`: Создаст страницы.
- `update_contacts_new.sql`: Заменит старый текст контактов на новый (с картой и новыми телефонами).

---

## 3. ПРОВЕРКА

После выполнения ВСЕХ шагов:
1. Очистите кеш браузера (Ctrl+F5).
2. Проверьте ссылки в меню - они должны открываться.
3. Проверьте кнопку корзины - она должна работать.
4. Проверьте футер - там не должно быть "Консультации" и дублей сертификатов.
5. Проверьте страницы "Аптеки" и "Контакты" - там должен быть текст.

---

**Файлы готовы:**
- `main_FIXED.tpl` (готовый шаблон)
- `fix_all_pages.sql` (исправление ссылок)
- `insert_missing_pages.sql` (создание отсутствующих страниц)
- `settings/conf_server.php` (конфиг)
- `settings/connect_server.php` (подключение)
