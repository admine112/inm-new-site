# Исходная конфигурация сайта (ДО изменений)

**Источник:** www.zip (оригинальный архив с production-сервера)  
**Дата архива:** 2023-06-06

---

## 📋 Конфигурационные файлы

### 1. settings/conf.php

**Оригинальная конфигурация базы данных:**

```php
$glb["db_host"]="is501201.mysql.ukraine.com.ua";
$glb["db_basename"]="is501201_inm";
$glb["db_user"]="is501201_inm";
$glb["db_password"]="(!keSB72a5";
```

**Параметры:**
- PHP версия: 5.x (судя по использованию `mysql_connect`)
- Сервер БД: `is501201.mysql.ukraine.com.ua`
- База данных: `is501201_inm`
- Пользователь: `is501201_inm`
- Пароль: `(!keSB72a5`

**Другие настройки:**
```php
$cms_ver="4.7.23b";
ini_set("display_errors", "On");
error_reporting(E_ERROR | E_WARNING | E_PARSE);
$global_meil="ower@ower.com.ua";
```

---

### 2. settings/connect.php

**Оригинальный код подключения:**

```php
<?php
  mysql_connect($glb["db_host"],$glb["db_user"],$glb["db_password"]) 
    or die ("Ошибка соединения с базой, проверьте настройки".mysql_error());
  mysql_select_db($glb["db_basename"]);
  mysql_query("SET NAMES UTF8");
?>
```

**Особенности:**
- Используется устаревшая функция `mysql_connect()` (PHP 5.x)
- Нет обработки ошибок, кроме `die()`
- Кодировка: UTF8

---

### 3. templates/shop1/main.tpl

**Оригинальное footer-меню (строка 528):**

```html
<a href="/">Главная</a>
<a href="/konsultacii">Консультации</a>
<a href="/videoperedachi">Видеопередачи</a>
<a href="/stati">Статьи</a>
<a href="/otzivi">Отзывы</a>
<a href="/apteki">Аптеки</a>
<a href="/kontakti">Контакты</a>
```

**Особенности:**
- Присутствует ссылка `/konsultacii` (которой нет в базе данных)
- Все ссылки используют латинские алиасы

---

## 🗄️ База данных (исходное состояние)

### Таблица: content_articles

**Проблемные записи (найдены при анализе):**

| ID | Название | Оригинальный alias | use_alias | Проблема |
|----|----------|-------------------|-----------|----------|
| 15 | Видеопередачи | `видеопередачи` (кириллица) | 0 | Не работает URL `/videoperedachi` |
| 25 | Отзывы | `отзывы` (кириллица) | 0 | Не работает URL `/otzivi` |
| 3 | Состав Инмунофлама | `состав-инмунофлама` (кириллица) | 0 | Не работает URL |
| 180 | Аптеки | `kontakti` (неправильный) | 1 | Неправильный alias |

**Примечание:** Страница "Консультации" (`/konsultacii`) отсутствует в базе данных, но ссылка на неё есть в footer.

---

## 📊 Структура товаров

### Таблица: shop_commodity

**Пример товара:**

| Поле | Значение |
|------|----------|
| commodity_ID | 1 |
| com_name | Инмунофлам |
| alias | inmunoflam |
| commodity_price | 2290 |
| cur_id | 1 |
| visible | 1 |

**URL товара:** `/pr1-inmunoflam/`

---

## 🔧 Модули и функциональность

### Корзина

**Файлы:**
- `modules/commodities/ajax/site.php` - AJAX-обработчик корзины
- `includes/js/cms.js` - JavaScript функции корзины

**Функция добавления в корзину:**
```javascript
function buy(id) {
    jQuery.getJSON('/modules/commodities/ajax/site.php?addtobasket=1&basket_com_id='+id+'&callback=?', 
        {}, function(booking) {
            // обработка ответа
        }
    );
}
```

**Обработчик на сервере (site.php, строки 252-266):**
```php
elseif($_GET["addtobasket"]==1) {
    if(is_numeric($_GET["basket_com_id"])) {
        $basket_items[$_GET["basket_com_id"]]+=$count;
        $_SESSION["basket_items"]=$basket_items;
        $res=1;
    }
    echo $_GET['callback']."({res:\"{$res}\"})";
}
```

---

## 📝 Выводы

### Что работало:
✅ Подключение к базе данных (на production-сервере с PHP 5.x)  
✅ Функциональность корзины  
✅ Отображение товаров  
✅ Некоторые страницы (со включенным `use_alias`)

### Что НЕ работало:
❌ Страницы с кириллическими алиасами (`/videoperedachi`, `/otzivi`)  
❌ Страница `/konsultacii` (отсутствует в БД)  
❌ Страницы с `use_alias=0`  
❌ Совместимость с PHP 7.4+ (для локальной разработки)

---

## 🔄 Что было изменено

См. файл: [CHANGELOG.md](file:///home/linuxuser/Загрузки/Лидия/Сайт/%20inmunoflam.com.ua(Резерв)/CHANGELOG.md)

---

**Дата создания отчёта:** 2025-11-22 18:47
