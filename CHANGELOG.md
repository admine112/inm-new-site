# Отчёт об изменениях сайта inmunoflam.com.ua

**Дата:** 2025-11-22  
**Статус:** В процессе развёртывания на production

---

## 📋 Краткое содержание

Выполнена полная диагностика и исправление сайта для корректной работы на production-сервере. Исправлены проблемы с подключением к базе данных, навигацией, отображением страниц и функциональностью корзины.

---

## ✅ Выполненные изменения

### 1. Конфигурация базы данных

#### `settings/conf.php`
**Проблема:** Использовалась Docker-конфигурация (`db_host="db"`) вместо серверной  
**Решение:** Создан `conf_server.php` с правильными настройками для production

```diff
- $glb["db_host"]="db";  // Docker
+ $glb["db_host"]="is501201.mysql.ukraine.com.ua";  // Production
```

**Файл для загрузки:** `www/www/settings/conf_server.php` → переименовать в `conf.php`

---

#### `settings/connect.php`
**Проблема:** Использовался `mysqli_connect()` (PHP 7.4+) вместо `mysql_connect()` (PHP 5.x)  
**Решение:** Создан `connect_server.php` с совместимостью для старого PHP

```diff
- mysqli_connect($glb["db_host"], $glb["db_user"], $glb["db_password"])  // PHP 7.4+
+ mysql_connect($glb["db_host"], $glb["db_user"], $glb["db_password"])  // PHP 5.x
```

**Файл для загрузки:** `www/www/settings/connect_server.php` → переименовать в `connect.php`

---

### 2. Исправление навигации

#### `templates/shop1/main.tpl`
**Проблема:** Ссылка на несуществующую страницу `/konsultacii` в подвале  
**Решение:** Удалена ссылка из footer-меню

```diff
- <a href="/konsultacii">Консультации</a>
```

**Строка:** 468  
**Файл для загрузки:** `www/www/templates/shop1/main.tpl`

---

### 3. Исправление алиасов страниц в базе данных

**Проблема:** Несколько страниц имели кириллические алиасы или отключенный `use_alias`, что приводило к 404 ошибкам

#### Исправленные страницы:

| ID | Название | Было | Стало | URL |
|----|----------|------|-------|-----|
| 15 | Видеопередачи | `alias='видеопередачи'`, `use_alias=0` | `alias='videoperedachi'`, `use_alias=1` | `/videoperedachi` |
| 25 | Отзывы | `alias='отзывы'`, `use_alias=0` | `alias='otzivi'`, `use_alias=1` | `/otzivi` |
| 3 | Состав Инмунофлама | `use_alias=0` | `alias='sostav-inmunoflama'`, `use_alias=1` | `/sostav-inmunoflama` |
| 180 | Аптеки | `alias='kontakti'` | `alias='apteki'`, `use_alias=1` | `/apteki` |

**SQL-скрипт для production:** `fix_all_pages.sql`

```sql
USE is501201_inm;

UPDATE content_articles SET alias='videoperedachi', use_alias=1 WHERE articleID=15;
UPDATE content_articles SET alias='otzivi', use_alias=1 WHERE articleID=25;
UPDATE content_articles SET alias='sostav-inmunoflama', use_alias=1 WHERE articleID=3;
UPDATE content_articles SET alias='apteki', use_alias=1 WHERE articleID=180;
```

---

### 4. Функциональность корзины

**Статус:** ✅ Проверено - функционал существует

**Файлы:**
- `modules/commodities/ajax/site.php` - обработчик `addtobasket` (строки 252-266)
- `includes/js/cms.js` - функция `buy(id)` (строки 495-510)

**Примечание:** Корзина уже работает в коде, дополнительных изменений не требуется

---

## 📦 Файлы для загрузки на сервер

### Обязательные файлы:

1. **settings/conf_server.php** → переименовать в `conf.php`
2. **settings/connect_server.php** → переименовать в `connect.php`
3. **templates/shop1/main.tpl** (с удалённой ссылкой "Консультации")

### SQL-скрипты для выполнения:

1. **fix_all_pages.sql** - исправление алиасов всех страниц

---

## ✅ Проверка после развёртывания

### Тестирование страниц (локально - все работают):

```
✅ /                      - HTTP 200 (Главная)
✅ /videoperedachi        - HTTP 200 (Видеопередачи)
✅ /stati                 - HTTP 200 (Статьи)
✅ /otzivi                - HTTP 200 (Отзывы)
✅ /apteki                - HTTP 200 (Аптеки)
✅ /kontakti              - HTTP 200 (Контакты)
✅ /sostav-inmunoflama    - HTTP 200 (Состав)
```

### Чеклист для production:

- [ ] Загружен `conf.php` с правильным `db_host`
- [ ] Загружен `connect.php` с `mysql_connect()`
- [ ] Загружен `main.tpl` без ссылки "Консультации"
- [ ] Выполнен SQL-скрипт `fix_all_pages.sql`
- [ ] Проверены все страницы из списка выше
- [ ] Проверена корзина - товары добавляются

---

## 🔍 Дополнительная информация

### Структура базы данных:

- **Таблица товаров:** `shop_commodity`
  - Товар ID=1: "Инмунофлам", alias="inmunoflam", цена=2290 грн
  - URL товара: `/pr1-inmunoflam/`

- **Таблица страниц:** `content_articles`
  - Отзывы (ID=25): 45 дочерних статей (отзывы пользователей)
  - Все основные страницы теперь имеют латинские алиасы

### Окружения:

| Параметр | Локально (Docker) | Production (Сервер) |
|----------|-------------------|---------------------|
| PHP версия | 7.4 | 5.x |
| MySQL функции | `mysqli_*` | `mysql_*` |
| DB host | `db` | `is501201.mysql.ukraine.com.ua` |
| Порт | 8090 | 80/443 |

---

## 📝 Примечания

1. **Не загружайте** локальные версии `conf.php` и `connect.php` на сервер - они настроены для Docker
2. **Используйте** файлы с суффиксом `_server` для production
3. **Выполните** SQL-скрипт через phpMyAdmin на сервере
4. **Проверьте** все страницы после развёртывания

---


---

## 🛒 Проблема: Ссылка на корзину не активна - ТРЕБУЕТ ДИАГНОСТИКИ

**Дата:** 2025-11-22 19:13  
**Статус:** ⚠️ Требует дополнительной диагностики на сервере

**Проблема:** На production-сервере ссылка на корзину в header не кликабельна

**Что проверено:**
- ✅ HTML идентичен оригиналу из `www.zip`
- ✅ Локально работает (HTTP 301 → 200)
- ✅ Страница `/basket` работает напрямую
- ✅ Добавлен `onclick` обработчик на `<div class="basket">`
- ❌ Кнопка всё равно не кликается на сервере

**Возможные причины:**
1. JavaScript на сервере активно блокирует все клики (event.stopPropagation)
2. CSS свойство `pointer-events: none` применяется через внешний файл
3. Поверх кнопки наложен невидимый элемент с большим z-index
4. Проблема специфична для конкретного браузера/устройства

**Рекомендация:**
Необходима диагностика непосредственно на сервере через DevTools:
1. Открыть F12 → Elements
2. Найти элемент `.basket` или `#basket`
3. Проверить вкладку "Computed" → `pointer-events`, `z-index`, `cursor`
4. Проверить вкладку "Event Listeners" → есть ли обработчики, блокирующие клик
5. Временно отключить все JavaScript файлы и проверить, работает ли кнопка

**Альтернативное решение:**
Если кнопка критична, можно добавить дублирующую текстовую ссылку "Перейти в корзину" в другом месте страницы.

---

**Последнее обновление:** 2025-11-22 19:15
